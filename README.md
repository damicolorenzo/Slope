# Slope

**Progetto di programmazione per il web**

Repository del progetto *Slope*, un’applicazione web sviluppata utilizzando **PHP, HTML, CSS e JavaScript**, realizzata come progetto didattico per il corso di programmazione web.

---

## 📖 Descrizione

Slope è un’applicazione web che combina logica **server-side** e **client-side** per offrire un’esperienza interattiva all’utente.  
Il progetto è pensato come esercitazione accademica e dimostra l’utilizzo di tecnologie web fondamentali, inclusa la gestione di un database relazionale.

---

## 📂 Struttura del progetto

```
Slope/
├── app/ # Codice applicativo principale
├── documentazione/ # Documentazione del progetto
├── libs/ # Librerie esterne
├── .gitignore
├── .htaccess # Configurazione Apache
├── index.php # Entry point dell’applicazione
├── slope.sql # Script SQL del database
└── README.md # Documentazione del repository
```

---

## ⚙️ Requisiti

- PHP 7.x o superiore
- Apache (XAMPP, MAMP, LAMP, ecc.)
- MySQL / MariaDB
- Browser web moderno

---

## 🚀 Installazione

1. **Clona il repository**
   ```bash
   git clone https://github.com/damicolorenzo/Slope.git

2. **Configura il server locale**
- Copia la cartella Slope nella directory del server web
(es. htdocs per XAMPP)
- Avvia Apache e MySQL

3. **Importa il database**
- Crea un database MySQL (es. slope)
- Importa il file slope.sql tramite phpMyAdmin

4. **Avvia l’applicazione**
```
http://localhost/Slope/
```

---

## 🧠 Funzionamento

- Backend: PHP gestisce la logica applicativa e l’accesso al database
- Frontend: HTML, CSS e JavaScript per interfaccia e interazioni
- Database: MySQL per la persistenza dei dati
- Apache: configurato tramite .htaccess

---

## 🛠 Tecnologie utilizzate

- PHP
- HTML5
- CSS3
- JavaScript
- MySQL / MariaDB

---

## 🗄 Database

Il file slope.sql contiene:
- Struttura delle tabelle
- Eventuali dati iniziali

Deve essere importato prima dell’utilizzo dell’applicazione.

---

# Slope

**Web programming project**

Repository for the *Slope* project, a web application developed using **PHP, HTML, CSS, and JavaScript**, created as an educational project for a web programming course.

---

## 📖 Description

Slope is a web application that combines **server-side** and **client-side** logic to provide an interactive user experience.  
The project is designed as an academic exercise and demonstrates the use of fundamental web technologies, including the management of a relational database.

---

## 📂 Project structure

```
Slope/
├── app/ # Codice applicativo principale
├── documentazione/ # Documentazione del progetto
├── libs/ # Librerie esterne
├── .gitignore
├── .htaccess # Configurazione Apache
├── index.php # Entry point dell’applicazione
├── slope.sql # Script SQL del database
└── README.md # Documentazione del repository
```

---

## ⚙️ Requirements

- PHP 7.x or higher
- Apache (XAMPP, MAMP, LAMP, etc.)
- MySQL / MariaDB
- Modern web browser

---

## 🚀 Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/damicolorenzo/Slope.git

2. **Set up the local server**
- Copy the Slope folder into the web server directory
(e.g. htdocs for XAMPP)
- Start Apache and MySQL

3. **Import the database**
- Create a MySQL database (e.g. slope)
- Import the slope.sql file using phpMyAdmin

4. **Run the application**
```
http://localhost/Slope/
```

---

## 🧠 How it works

- Backend: PHP handles application logic and database access
- Frontend: HTML, CSS, and JavaScript for the interface and interactions
- Database: MySQL for data persistence
- Apache: configured via .htaccess

---

## 🛠 Technologies used
- PHP
- HTML5
- CSS3
- JavaScript
- MySQL / MariaDB

## 🗄 Database

The slope.sql file contains:
- Table structure
- Any initial data

It must be imported before using the application.
